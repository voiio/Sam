{
  "name": "Sam",
  "description": "A GPT based slack bot trained to specific personalities and use cases.",
  "repository": "https://github.com/voiio/Sam",
  "logo": "https://upload.wikimedia.org/wikipedia/commons/0/04/ChatGPT_logo.svg",
  "keywords": [
    "OpenAI",
    "Slack",
    "bot",
    "GPT",
    "AI"
  ],
  "website": "https://github.com/voiio/Sam",
  "env": {
    "SLACK_APP_TOKEN": {
      "description": "Your Slack app's token, starting with 'xapp-'",
      "required": true
    },
    "SLACK_BOT_TOKEN": {
      "description": "Your Slack bot's token, starting with 'xoxb-'",
      "required": true
    },
    "OPENAI_API_KEY": {
      "description": "Your OpenAI API key.",
      "required": true
    },
    "OPENAI_ASSISTANT_ID": {
      "description": "Your OpenAI assistant ID, starting with 'asst_'",
      "required": true
    },
    "RANDOM_RUN_RATIO": {
      "description": "The ratio of random GPT runs in Group Chats w/o mentions. 0.5 means 50% of the time, the bot will run a random response, and 50% of the time, the bot will run a GPT response.",
      "required": false,
      "value": "0.1"
    }
  },
  "formation": {
    "bot": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "addons": [
    "heroku-redis:mini"
  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
